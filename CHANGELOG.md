## Fixes

* **Git URL Suffix Handling**: Replaced string replacement calls with `removesuffix()` for cleaner removal of `.git` suffixes from repository URLs and names (`[626512c](https://github.com/aquiveal/rune/commit/626512c9)`).
* **Test Fixture URL Correction**: Fixed a URL typo in the test fixtures to ensure accurate repository parsing (`[3d5c889](https://github.com/aquiveal/rune/commit/3d5c889a)`).
